spring:
    application:
        name: library
    datasource:
        url: jdbc:mysql://localhost:3306/library
        username: root
        password: 

    liquibase:
        change-log: classpath:db/changelog/db.changelog-master.yaml