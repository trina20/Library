spring:
    application:
        name: library
    datasource:
        url: jdbc:mysql://localhost:3306/mydb
        username: root
        password: password

    liquibase:
        change-log: classpath:db/changelog/db.changelog-master.yaml